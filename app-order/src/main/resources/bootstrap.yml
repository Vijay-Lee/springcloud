server:
  port: 9092

spring:
  application:
    name: app-order
  cloud:
    config:
      label: master
      profile: dev
      uri: http://localhost:8888/
  zipkin:
    base-url: http://localhost:9411